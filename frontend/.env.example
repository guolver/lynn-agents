# true: use the built-in visualization dataset and simulated action results.
# false: read and invoke the configured Agent Hub backend.
AGENT_HUB_DEMO_MODE=true
AGENT_HUB_API_URL=http://127.0.0.1:8000
